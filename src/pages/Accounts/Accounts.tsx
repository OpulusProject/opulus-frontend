import React from 'react';

import { Typography } from '@/components/Typography';

export const Accounts: React.FC = () => {
  return <Typography variant="extra-extra-large">Accounts</Typography>;
};
